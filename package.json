{
  "name": "multi-timezone-clock",
  "version": "1.0.0",
  "engines": {"node":"22.x"},
  "description": "Multi Timezone Clock Assignment",
  "scripts": {
  "build:client": "tsc -p tsconfig.client.json",
  "build:server": "tsc -p tsconfig.server.json",
  "build": "npm run build:client && npm run build:server",
  "dev": "npm run build && node dist/server/server.js"
},
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^22.15.30",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}